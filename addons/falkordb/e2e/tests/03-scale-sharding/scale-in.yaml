apiVersion: operations.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: fdb-scale-in-sharding
spec:
  clusterName: fdb-shard
  type: HorizontalScaling
  horizontalScaling:
    - componentName: shard
      shards: 3
